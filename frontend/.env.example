# Backend URL used by the Next.js proxy to rewrite /api/v1/* requests.
# Read at runtime (not baked at build time). Defaults to http://localhost:8000 for local dev.
# In Docker set this to the internal service name: BACKEND_URL=http://backend:8000
# BACKEND_URL=http://localhost:8000

# Auth mode: clerk or local (runtime, not baked at build time).
# - clerk: Clerk sign-in flow
# - local: shared bearer token entered in UI
AUTH_MODE=local

# Clerk auth (used when AUTH_MODE=clerk)
CLERK_PUBLISHABLE_KEY=
CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/boards
CLERK_AFTER_SIGN_OUT_URL=/
