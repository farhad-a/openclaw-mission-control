# Backend URL used by the Next.js server to proxy /api/v1/* requests.
# Read at runtime (not baked at build time). Defaults to http://localhost:8000 for local dev.
# In Docker set this to the internal service name: BACKEND_URL=http://backend:8000
# BACKEND_URL=http://localhost:8000

# Auth mode: clerk or local.
# - clerk: Clerk sign-in flow
# - local: shared bearer token entered in UI
NEXT_PUBLIC_AUTH_MODE=local

# Clerk auth (used when NEXT_PUBLIC_AUTH_MODE=clerk)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/boards
NEXT_PUBLIC_CLERK_AFTER_SIGN_OUT_URL=/
